apply plugin: 'android-library'

android {
    compileSdkVersion 15
    buildToolsVersion "17.0"
}

dependencies {
    compile project(':baseLibrary')
    compile files("$rootDir/android-support-v4.jar")
}
