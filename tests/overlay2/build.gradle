buildscript {
    repositories {
        maven { url '../../repo' }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.3-SNAPSHOT'
    }
}

apply plugin: 'android'

android {
    compileSdkVersion 15

    productFlavors {
        one {}
    }
}