apply plugin: 'android-library'

dependencies {
    compile project(':lib2:libs')
}

android {
    compileSdkVersion 15
    buildToolsVersion "17.0"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 15
    }
}