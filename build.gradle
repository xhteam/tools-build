allprojects {
    apply plugin: 'idea'

    repositories {
        mavenCentral()
    }

    project.ext {
        baseVersion = '0.3'
    }

    group = 'com.android.tools.build'
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.3'
}
